# Saga Pattern

This pattern coordinates multi-step distributed transactions with compensating operations. Step sequencing and compensation logic are defined in the workflow manifest. State persistence requirements apply to the coordinating layer.
