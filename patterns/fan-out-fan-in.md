# Fan-Out / Fan-In

This pattern manages parallel branch execution and result aggregation across distributed workflow steps. Completion tracking and aggregation strategy are configured at the workflow definition level. Partial failure behavior is governed by per-workflow thresholds.
